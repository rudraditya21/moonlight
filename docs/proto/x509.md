# X509 Protocol

## Location
`network/proto/src/x509/`

## Overview
This module provides a full X509 protocol implementation intended for use by Moonlight modules. Use the public types and functions below as the stable entry points.

## Public Types
### Structs
- `AsyncX509Client`
- `AsyncX509Server`
- `X509Certificate`
- `X509Client`
- `X509ClientConfig`
- `X509Name`
- `X509Server`
- `X509ServerConfig`
- `X509Validity`

## Key Entry Points
### Clients
- `AsyncX509Client`
- `X509Client`
- `X509ClientConfig`

### Servers
- `AsyncX509Server`
- `X509Server`
- `X509ServerConfig`

### Free Functions
- `pub fn parse_certificate(der: &[u8]) -> CoreResult<X509Certificate> {`

### Methods
- `AsyncX509Client`
  - `pub async fn connect(addr: &NetAddr, config: X509ClientConfig) -> CoreResult<Self>`
  - `pub async fn get_certificate(&mut self) -> CoreResult<Vec<u8>>`
  - `pub async fn parse_remote(&mut self, der: &[u8]) -> CoreResult<String>`
- `AsyncX509Server`
  - `pub async fn bind(addr: SocketAddr, config: X509ServerConfig) -> CoreResult<Self>`
  - `pub async fn serve(&self) -> CoreResult<()>`
- `X509Client`
  - `pub fn connect(addr: &NetAddr, config: X509ClientConfig) -> CoreResult<Self>`
  - `pub fn get_certificate(&mut self) -> CoreResult<Vec<u8>>`
  - `pub fn parse_remote(&mut self, der: &[u8]) -> CoreResult<String>`
- `X509Server`
  - `pub fn bind(addr: SocketAddr, config: X509ServerConfig) -> CoreResult<Self>`
  - `pub fn local_addr(&self) -> CoreResult<SocketAddr>`
  - `pub fn serve(&self) -> CoreResult<()>`

## Usage Notes
- Start with the client or server structs listed above, then follow their constructors and connect/bind/listen methods if present.
- Encode and decode helpers are exposed as public functions or methods in this module; use them to build protocol frames.
- When in doubt, search for the types above in the code to find concrete examples and tests in the same module directory.

## Tests and Examples
- Unit tests live alongside the implementation in `network/proto/src/x509/`.
- Protocol‑level fuzz/negative tests are located in `network/proto/tests/negative_fuzz.rs` where applicable.

